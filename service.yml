apiVersion: v1
kind: Service
metadata:
  name: service
  labels:
    env: app
    key: version
spec:
  type: LoadBalancer
  selector:
    env: app
    key: version
  ports:
    - port: 8080
      targetPort: 8080

